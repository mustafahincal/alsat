export const setToLocalStorage = (key, data) => localStorage.setItem(key, data);
export const getFromLocalStorage = (key) => localStorage.getItem(key);
export const removeFromLocalStorage = (key) => localStorage.removeItem(key);
